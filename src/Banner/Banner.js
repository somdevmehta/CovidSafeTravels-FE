import React from 'react';

const Banner = ({ source, destination }) => {
    return (<div class="banner eEqiTz">You are flying from {source} to {destination}, CovidSafeTravels Info will appear here ...</div>);
}

export default Banner;